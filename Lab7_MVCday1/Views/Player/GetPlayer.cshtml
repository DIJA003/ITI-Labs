@model Lab7_MVCday1.DTO.PlayerDTO

<div class="card">
    <div class="card-header">
        Create Player
    </div>
    <div class="card-body">
        @if (ViewBag.Message != null)
        {
            <div class="alert alert-danger" role="alert">
                @ViewBag.Message
            </div>
        }
        else
        {
            <form>

                @if (ViewBag.ErrorMessage != null)
                {
                    <div class="alert alert-danger" role="alert">
                        @ViewBag.ErrorMessage
                    </div>
                }
                <input type="hidden" asp-for="Id" />
                <div class="form-group mb-3">
                    <label for="exampleInputEmail1">Name</label>
                    <input readonly asp-for="Name" type="text" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="Enter email">

                </div>
                <div class="form-group mb-3">
                    <label for="exampleInputPassword1">Age</label>
                    <input readonly asp-for="Age" type="text" class="form-control" id="exampleInputPassword1" placeholder="Enter Age">
                </div>
                <div class="form-group mb-3">
                    <label for="exampleInputPassword1">Email</label>
                    <input readonly asp-for="Email" type="text" class="form-control" id="exampleInputPassword1" placeholder="Enter Email">
                </div>
                <div class="form-group mb-3">
                    <label for="exampleInputPassword1">Password</label>
                    <input readonly asp-for="Password" type="text" class="form-control" id="exampleInputPassword1" placeholder="Enter Password">
                </div>

                <a asp-controller="Player" asp-action="Index" class="btn btn-success mt-2">Return</a>
                <a asp-controller="Player" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-info mt-2">Edit</a>
                <a asp-controller="Player" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger mt-2">Delete </a>
            </form>

        }

    </div>
</div>